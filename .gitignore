# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
venv-somali/
env/
ENV/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data (don't commit large files - regenerate via pipelines)
data/*
!data/.gitkeep
!data/metrics/        # Keep metrics JSON files (small, valuable for dashboard)
!data/reports/        # Keep quality reports (markdown, useful for reference)
data/silver/*         # Exclude processed data files
data/bronze/*         # Exclude raw data files
data/gold/*           # Exclude final datasets
*.pkl
*.h5
*.parquet
*.jsonl              # Exclude large JSONL data files

# Models (use Git LFS or external storage)
models/*.bin
models/*.pt
*.safetensors

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs (runtime artifacts)
logs/*
!logs/.gitkeep
*.log

# Secrets
.env
*.key

# Development artifacts (archived planning documents)
.archive/
