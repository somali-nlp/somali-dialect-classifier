# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
venv-somali/
env/
ENV/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data (don't commit large files - regenerate via pipelines)
# Explicitly ignore subdirectories except metrics and reports
data/silver/          # Exclude processed data files
data/bronze/          # Exclude raw data files
data/gold/            # Exclude final datasets
data/staging/         # Exclude staging data files
data/processed/       # Exclude processed data files
data/raw/             # Exclude raw data files
data/ledger/          # Exclude crawl ledger database
data/analysis/        # Exclude analysis artifacts
# Keep metrics and reports for dashboard (small files)
# data/metrics/ - JSON metrics files
# data/reports/ - Quality reports (markdown)
*.pkl
*.h5
*.parquet
*.jsonl              # Exclude large JSONL data files

# Dashboard cache
dashboard/data/       # Exclude dashboard cache files

# Models (use Git LFS or external storage)
models/*.bin
models/*.pt
*.safetensors

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs (runtime artifacts)
logs/*
!logs/.gitkeep
*.log

# Lock files (distributed locking for concurrent runs)
.locks/
*.lock

# Secrets
.env
*.key

# Development artifacts (archived planning documents)
.archive/

# GitHub Pages build artifacts
_site/

# Local testing artifacts (Playwright, etc.)
.playwright-mcp/
playwright.config.js
test-results/
tests/screenshots/
tests/*-navigation.js
tests/test_dashboard_calculation_unit.py
scripts/analyze_success_rate.py
dashboard/index-advanced.html
dashboard/js/demo-*.js
design-system-*.html

# TikTok integration testing directory
tiktok_apify_test/
node_modules/
*-snapshots/

# Claude Code configuration
.claude/
